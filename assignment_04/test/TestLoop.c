int foo(int a, int b, int x, int n) {

    for (int i = 0; i < a; ++i) {
        int f = 3 * a;
    }
 
    // int l = x*n; // uncomment for not adjacent loops

    while(x != 100){
          x = b*2;
    }


  int i = 0;
  if (n > 0) {
    do {
      // Loop body
      i += 1;
    } while (i < n);
  }

  int k = 99;
  if (n > 0) {
    do {
      // Loop body
      k += 1;
    } while (k < n);
  }

    return 0;
}