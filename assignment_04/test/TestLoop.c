int foo(int a, int b, int x, int n) {

    // for (int i = 0; i < a; ++i) {
    //     int f = 3 * a;
    // }

    // if (a == b) {
        
    //     while(x != 100){
    //         x = b*2;
    //     }
    // }

    // x = a *b;

    // while( a == 200){
    //     int b = b * 10;
    // }
    

    int i = 0;
  if (n > 0) {
    do {
      // Loop body
      i += 1;
    } while (i < n);
  }

  while (x != 1) {
    n = a * b;
  }
  while (x != 1) {
    n = a * b;
  }
    return 0;
}